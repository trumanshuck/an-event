<section>
  <h1
    class="text-6xl font-bold font-title py-40 text-center text-gray-200 bg-no-repeat bg-center bg-cover"
    style="background-image:
            linear-gradient(
              rgba(0, 0, 0, 0.45),
              rgba(0, 0, 0, 0.45)
            ),
            url(<%= image_url("dunes.jpeg") %>)
          "
    >
    <div>
      Where We've Been
    </div>
    <div>&nbsp;</div>
  </h1>

  <div class="max-w-4xl mx-auto px-4">
    <div class="prose mt-32 mb-16 max-w-4xl">
      <p class="text-lg md:text-2xl pb-6">
        We'd never been to any of these places before. We've got some thoughts on what it was like to visit. Spoiler: mostly fun!
      </p>
    </div>

    <div class="grid grid-cols-2 gap-2 bg-white p-4 border-gray-200">
      <% @stops.each do |stop| %>
        <%= link_to url_for(stop), class: "flex relative block justify-center items-center" do %>
          <%= image_tag stop.splash, class: "object-cover w-full h-full" %>

          <div class="flex flex-col justify-center items-center absolute text-white inset-0 z-auto" style="background-image:
            linear-gradient(
              rgba(0, 0, 0, 0.45),
              rgba(0, 0, 0, 0.45)
            )">
            <span class="text-4xl text-white">
              <%= stop.name %>
            </span>
            <span>
              <%= stop.from.strftime("%B %-d, %Y") %>
            </span>
            <span>
              <%= stop.to.strftime("%B %-d, %Y") %>
            </span>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</section>
